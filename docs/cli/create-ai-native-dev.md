# create-ai-native-dev

`npx create-ai-native-dev` はNode以外のプロジェクトにも使える**導入CLI**。

- 環境本体（compiler/runtime/package manager）はインストールしない。
- プロジェクト種別を検出し、Stack Profileに応じたテンプレートを導入する。
- npxは配布経路であり、導入対象はNode.js限定ではない。

参照:
- `docs/cli/stack-profile-strategy.md`
- `docs/cli/safety-layer.md`
