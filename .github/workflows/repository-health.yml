name: repository-health
on:
  pull_request:
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 雛形チェック
        run: |
          echo "repository-health.yml の雛形です"
          echo "TODO: 実運用ツールを組み込む"
