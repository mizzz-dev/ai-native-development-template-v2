name: docs
on:
  pull_request:
    paths: ['docs/**','README.md','CONTRIBUTING.md','SECURITY.md']
jobs:
  docs-validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/validate-docs.sh
